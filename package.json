{
  "name": "matti-client-js",
  "version": "0.0.0",
  "description": "Client libray for matti service",
  "main": "index.js",
  "scripts": {
    "test": "jest",
    "build": "babel src --ignore test.js --out-dir dist/",
    "prepublish": "npm run build"
  },
  "dependencies": {
    "grpc": "1.1.2"
  },
  "devDependencies": {
    "babel-cli": "6.24.1",
    "babel-preset-es2015": "^6.13.2",
		"babel-preset-stage-0": "^6.5.0",
    "jest": "19.0.2"
  },
  "files": [
    "dist/",
    "lib/",
    "index.js"
  ]
}